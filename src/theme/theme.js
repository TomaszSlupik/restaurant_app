import { createTheme } from '@mui/material/styles';

const themeColor = createTheme({
    palette: {
      primary: {
        main: "#2a52be",
      }, 
      secondary: {
        main: "#008080 ",
      }
    },
  });
  
  export default themeColor