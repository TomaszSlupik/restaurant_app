import { createTheme } from '@mui/material/styles';

const themeColor = createTheme({
    palette: {
      primary: {
        main: "#926F33",
      }, 
      secondary: {
        main: "#ffd536",
      }
    },
  });
  
  export default themeColor